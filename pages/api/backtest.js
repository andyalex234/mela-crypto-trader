

export default function handler(req, res) {

    //get scence

    //get see if there is a backtest run in the database with the same scene
    
    // if exist in the database return with result


    // if not produce a message (i.e. scene ) on a NewBacktest topic f



  res.status(200).json({ name: 'John Doe' })
}

//from the clinet/frontend we week listening via websockets from
//our node.js server. While listening/consuming from to the topic 
// when we find the id from the user 



//backtesting
//

